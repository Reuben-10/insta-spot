@import url("https://fonts.googleapis.com/css2?family=DM+Mono:ital,wght@0,300;0,400;0,500;1,300;1,400;1,500&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap");

@import "tailwindcss";

@theme {
  --breakpoint-xs: 21.4rem;
  --breakpoint-sm: 30rem;
  --breakpoint-md: 38rem;
  --breakpoint-lg: 55rem;
  --breakpoint-xl: 64rem;
}

@layer base {
  * {
    margin: 0;
    padding: 0;
    box-sizing: inherit;
  }

  html {
    box-sizing: border-box;
  }

  body {
    background: rgba(252, 245, 229, 1);
    font-family: "Poppins", sans-serif;
    font-weight: 400;
    font-size: 1rem;
    line-height: 1;
    color: #212121;
    overflow-x: hidden;
  }
}


@layer components {
  .logo-container {
    background: rgba(255, 255, 255, 0.8);
    @apply flex flex-col justify-center items-center w-full p-2;
  }

  .logo {
    @apply flex gap-2 no-underline text-[#212121];
  }

  /* .logo-text {
    font-family: "DM Mono", monospace;
    @apply font-medium text-[1.25rem] tracking-[0.25rem] uppercase;
  } */

  .banner {
    background: rgba(252, 245, 229, 1);
    @apply flex lg:flex-row justify-between items-end py-[1.875rem] pb-[1.25rem] max-w-[95%] mx-auto border-b border-[#21212166] ;
    /* xs:flex-col xs:justify-center xs:gap-8 xs:pb-[1.8rem] */
    /* xs:items-center */
  }

  .avatar-section {
    @apply flex lg:flex-row items-start gap-5;
    /* xs:flex-col xs:items-center xs:justify-center */
  }

  .avatar-img {
    @apply w-[11.875rem] h-[11.875rem] rounded-[0.625rem] object-fill;
  }

  .properties {
    @apply flex flex-col justify-between p-0 gap-[0.75rem] h-[11.875rem];
    /* xs:items-center xs:h-auto xs:gap-6 */
  }

  .details {
    @apply flex flex-col;
    /* xs:items-center */
  }

  .name {
    @apply font-medium text-[2rem] text-[#212121] mb-2 max-w-[22ch] overflow-hidden text-ellipsis line-clamp-3;
    /* xs:max-w-[50ch] xs:text-xl xs:py-0 xs:px-[30px] xs:text-center */
  }

  .description {
    color: rgba(33, 33, 33, 0.7);
    @apply font-medium text-[1rem] mb-2 max-w-[45ch] break-all overflow-hidden text-ellipsis line-clamp-3;
    /* sm:max-w-[65ch] sm:my-0 sm:mx-auto xs:text-[1rem] xs:py-0 xs:px-[2.2rem]; */
  }

  .btn {
    @apply flex justify-center items-center gap-2 cursor-pointer border border-transparent text-[1rem] font-medium transition-all ease-in-out duration-200;
    font-family: inherit;
  }

  .btn-light {
    @apply w-fit bg-transparent opacity-70;
  }

  .btn-light:hover,
  .btn-light:active {
    @apply opacity-100;
  }

  .btn-dark {
    @apply bg-[#212121] text-[#fcf5e5] px-[2rem] py-[1.125rem] rounded-lg;
    /* lg:w-auto xs:p-6 xs:mx-auto */
    /* xs:w-[90%] xs:mx-auto xs:p-6 */
  }

  .btn-dark:hover,
  .btn-dark:active {
    @apply bg-[#212121cc];
  }

  .main {
    @apply lg:max-w-[95%] lg:mx-auto md:max-w-[80%] xs:max-w-[85%] xs:mx-auto
  }

  .container {
    @apply grid lg:grid-cols-3 gap-y-12 gap-x-[1.3rem] py-5 pb-10 w-full border-b border-[#212121a6];
    /* sm:grid-cols-2 sm:gap-8 xs:grid-cols-1 xs:gap-20 */
  }

  .card-img-container {
    @apply w-full lg:max-h-[26.25rem] h-full rounded-[0.625rem] overflow-hidden transition-all duration-300 ease-in-out;
    /* xs:max-h-60 */
  }

  .card-img:hover{
    transform: scale(1.1);
    transition: transform 0.3s ease-in-out;
    overflow: hidden;
  }

  .card-img {
    @apply w-full h-full object-cover;
  }

  .card-description {
    @apply flex justify-between gap-[0.3125rem] pt-4;
    /* lg:pt-6 */
  }

  .card-title {
    @apply font-medium leading-[1.2] text-[1.25rem];
  }

  .card.focused .card-img {
    transform: scale(1.1);
  }
}